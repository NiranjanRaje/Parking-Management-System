spring.application.name=Parking Management System


#spring.datasource.url=jdbc:mysql://localhost:3306/example1?useSSL=false&serverTimezone=UTC
#spring.datasource.username=root
#spring.datasource.password=root
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

spring.liquibase.change-log=classpath:changelog.xml
spring.liquibase.enabled=true

# Dynamic port binding for Render
server.port=${PORT:8080}

# Optional: Let Spring use environment variables (these will override local ones)
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

# Optional JPA settings
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:true}




spring.mvc.hiddenmethod.filter.enabled=true